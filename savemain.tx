#include "includes.hpp"

template <class ctn>
void print(ctn & c)
{
	typename ctn::iterator itr = c.begin();
	for (itr != c.end(), ++itr)
	{
		std::cout << *itr << " ";
	} std::cout << std::endl;
}

int		main(void)
{
	ft::vector<int> test(10, 42);
	return (0);
}
